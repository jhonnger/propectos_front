/**
 * E2E — Por cerrar (Slice 1.4).
 *
 * La funcionalidad "Por cerrar" fue eliminada del frontend en CF-1.
 * Los endpoints /api/cierre/* ya no existen en el backend.
 * Este archivo se conserva como placeholder vacío para que el runner
 * no falle por archivo inexistente.
 */

// No hay tests — funcionalidad eliminada junto con el backend.
